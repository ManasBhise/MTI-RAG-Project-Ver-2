# ==============================================================================
# MTI Knowledge Assistant - Production Docker Compose Configuration
# ==============================================================================

services:
  # Backend API & RAG Vector Engine
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: mti-assistant-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - path: ./backend/.env
        required: false
      - path: ./.env
        required: false
    environment:
      - PYTHONUNBUFFERED=1
      - RAG_DATA_DIR=/app/data
      - RAG_VECTOR_STORE_DIR=/app/rag/store
    volumes:
      - ./data:/app/data
      - ./rag/store:/app/rag/store
      - mti_db_data:/app/backend/data
      - ./mti_assistant.db:/app/mti_assistant.db
    networks:
      - mti-network

  # Frontend UI & Nginx Reverse Proxy (Exposed to IMD Apache Web Server)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: mti-assistant-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - mti-network

volumes:
  mti_db_data:
    driver: local

networks:
  mti-network:
    driver: bridge
